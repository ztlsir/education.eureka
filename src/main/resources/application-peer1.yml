server:
  port: 80

eureka:
  instance:
    hostname: peer1
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://eureka.peer2/eureka/,http://eureka.peer3/eureka/